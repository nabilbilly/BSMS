[build]
builder = "nixpacks"

[deploy]
startCommand = "celery -A worker.tasks worker --beat --loglevel=info & uvicorn main:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*'"
preDeployCommand = "alembic upgrade head"
healthCheckPath = "/"
healthCheckTimeout = 100
